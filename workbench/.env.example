# CORE Workbench local admin configuration
#
# Do not commit real secrets.
# Generate the password hash with:
#   uv run python -m workbench.auth hash-password
# Generate the session secret with:
#   python - <<'PY'
#   import secrets
#   print(secrets.token_urlsafe(48))
#   PY

CORE_WORKBENCH_ADMIN_EMAIL=shayj292@gmail.com
CORE_WORKBENCH_ADMIN_PASSWORD_HASH=<paste-password-hash-here>
CORE_WORKBENCH_SESSION_SECRET=<paste-random-session-secret-here>
CORE_WORKBENCH_HOST=127.0.0.1
CORE_WORKBENCH_PORT=8765
CORE_WORKBENCH_UI_HOST=127.0.0.1
CORE_WORKBENCH_UI_PORT=5173
